# No API keys required!
# This app uses free, open-source services:
#   - Nominatim (geocoding) - no API key needed
#   - Overpass API (pub search) - no API key needed
#   - OSRM (routing) - no API key needed
#   - OpenStreetMap tiles (via Leaflet) - no API key needed
#
# If you want to customize or use different services, you can add configuration here.
# For example, if you want to use a custom OSRM instance:
# REACT_APP_OSRM_URL=https://your-osrm-instance.com
